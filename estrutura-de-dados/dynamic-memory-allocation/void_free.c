#include <stdio.h>
#include <stdlib.h>

int main(){
    //  Essa função libera o espaço alocado, isto é, libera a área de
    //  memória previamente alocada dinamicamente
    void free (void *ptr);

    return 0;
}